<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_gradient_start"
    tools:context=".pertemuan4.PinkMotivationActivity">

    <!-- HEADER / BANNER -->
    <ImageView
        android:id="@+id/imgBanner"
        android:layout_width="0dp"
        android:layout_height="220dp"
        android:scaleType="centerCrop"
        android:src="@drawable/gambarpinkkk"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- CARD UTAMA -->
    <androidx.cardview.widget.CardView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:cardCornerRadius="24dp"
        app:cardElevation="10dp"
        android:layout_margin="20dp"
        app:layout_constraintTop_toBottomOf="@id/imgBanner"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp">

            <!-- JUDUL -->

            <!-- DESKRIPSI -->
            <TextView
                android:id="@+id/tvJudul"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Pink Motivation"
                android:textColor="@color/primary_pink"
                android:textSize="22sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tvDeskripsi"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Temukan semangat dan motivasi harianmu 💖"
                android:textColor="@color/text_secondary"
                android:layout_marginBottom="16dp"/>

            <!-- GARIS PEMBATAS -->
            <View
                android:layout_width="60dp"
                android:layout_height="3dp"
                android:background="@color/primary_pink"
                android:layout_marginBottom="20dp"/>

            <!-- QUOTE (FOCUS UTAMA) -->
            <TextView
                android:id="@+id/tvQuote"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="“Jangan menyerah, semua butuh proses.”"
                android:textSize="20sp"
                android:textStyle="italic"
                android:textColor="@color/text_primary"
                android:gravity="center"
                android:layout_marginBottom="16dp"/>

            <!-- BOX MOTIVASI -->
            <TextView
                android:id="@+id/tvExtra"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Kamu hebat 💖"
                android:textSize="16sp"
                android:textColor="@color/text_white"
                android:background="@color/primary_pink_light"
                android:padding="12dp"
                android:gravity="center"
                android:layout_marginTop="8dp"/>

        </LinearLayout>

    </androidx.cardview.widget.CardView>

</androidx.constraintlayout.widget.ConstraintLayout>